# Supabase CLI — https://supabase.com/docs/guides/cli
# Stripe cannot send a Supabase JWT; disable JWT verification for this function only.

[functions.stripe-webhook]
verify_jwt = false

# Twilio posts form bodies — no Supabase JWT.
[functions.twilio-whatsapp-inbound]
verify_jwt = false
